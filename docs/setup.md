# DijiOne — Local Setup

**Superseded by Phase 2.5.** This document described the Phase 1/2
single-app setup (`apps/web` + `apps/api`), which no longer exists —
DijiOne is now eight independently runnable services. See
**`docs/platform/local-development.md`** for current setup instructions
(prerequisites, ports, `npm run dev:all`, seeding, troubleshooting) and
**`docs/platform/service-architecture.md`** for what each service is.
